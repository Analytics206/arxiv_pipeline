# Compatibility reference for non-Docker installs. The Docker image resolves
# the equivalent core + `agent` extra from the repository uv.lock.

# Core dependencies
pyyaml>=6.0.3,<7
click>=8.4.2,<9
aiohttp>=3.14.3,<4
python-dotenv>=1.2.2,<2

# Database clients
pymongo>=4.17.0,<5
neo4j>=6.2.0,<7

# Git integration
GitPython>=3.1.57,<4

# For code parsing
ast-comments>=1.3.0,<2

# Vector search
qdrant-client>=1.18.0,<2

# Progress reporting
tqdm>=4.69.1,<5
